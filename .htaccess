RewriteEngine On

# Route ALL requests to the biometric listener if the file doesn't exist
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /Files/api/bio_listener.php [L,QSA]
